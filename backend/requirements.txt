# ─────────────────────────────────────────────
# Episode 1 — Task Manager API
# Python dependencies
# ─────────────────────────────────────────────

# Django — the web framework
Django==4.2.16

# Django REST Framework — turns Django into a REST API
djangorestframework==3.15.2

# PostgreSQL adapter — lets Django talk to PostgreSQL
# psycopg2-binary installs cleanly on Linux (our Docker container)
psycopg2-binary==2.9.9

# python-dotenv — loads .env file into environment variables
python-dotenv==1.0.1

# Django Filter — adds ?status=todo style filtering to API
django-filter==24.3

# CORS Headers — allows your future React frontend to call this API
django-cors-headers==4.4.0